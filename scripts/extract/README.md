# Extraction Scripts

Place reviewed scripts here that obtain or transform public source material into
reproducible intermediate or generated outputs. Each script should retain source
IDs, version or commit information, inputs, outputs, and a reviewable change
report.

Do not run downloaded extraction code without inspection, and do not directly
overwrite curated runtime data.
